# Віртуальне середовище Python
venv/
env/

# Системні файли та кеш Python
__pycache__/
*.py[cod]
*$py.class

# Локальна база даних (для MVP ми будемо використовувати SQLite, її не можна пушити в репозиторій)
db.sqlite3

# Змінні середовища (тут будуть паролі, секретні ключі Django)
.env

# Медіафайли платформи (PDF навчальні матеріали, зображення), вони завантажуються користувачами, а не є частиною коду
media/

# Статичні файли, що генеруються автоматично
staticfiles/